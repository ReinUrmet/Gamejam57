using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    public int MoveSpeed;

    // Update is called once per frame
    void Update()
    {
        Move()
    }

    void Move()
    {
        transform
    }
}
